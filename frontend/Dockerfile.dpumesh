FROM gcc:13-bookworm AS build
WORKDIR /build
COPY common/ common/
COPY frontend/main_dpumesh.c .
RUN gcc -O2 -Wall -Icommon -o server main_dpumesh.c common/*.c

FROM debian:bookworm-slim
WORKDIR /app
COPY --from=build /build/server .
ENV APP=frontend
EXPOSE 8080
CMD ["./server"]
